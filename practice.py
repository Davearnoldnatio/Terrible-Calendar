import tkinter as tk

root = tk.Tk()  #created the window that the GUI is displayed on

greetings = tk.Label(root, text = "hi") #sets greeting to a label that says hi
greetings.pack() #puts the label on the window













root.mainloop()